# MD-WGPU

A simple renderer based on wgpu-rs. This can render simple shapes and text extremely fast by batching calls and using the bare minimum of render passes. Unfortunately this reduces flexibility. Probably not too useful for most people.
